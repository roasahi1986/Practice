# Enable Bzlmod (default in Bazel 7.x, but explicit for clarity)
common --enable_bzlmod

# Build options
build --cxxopt=-std=c++20
build --host_cxxopt=-std=c++20

# Test options
test --test_output=errors
